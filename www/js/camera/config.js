/**
    @module rateMe/camera
    @description Module that handles anything deadling with the camera, as well as uploading pictures from the Camera to the server
*/
(function (window) {
    'use strict';
    var angular = window.angular;
    
    
    
    angular.module('rateMe.camera', []);
    
    
    
}(window));