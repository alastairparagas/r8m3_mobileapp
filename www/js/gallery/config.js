/**
    @module rateMe/gallery
    @description Module that handles anything with presentation of pictures from our server, downloading such images, rating images, and so forth.
*/
(function (window) {
    'use strict';
    var angular = window.angular;
    
    
    angular.module('rateMe.gallery', []);
    
    
    
}(window));