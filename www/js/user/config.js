/**
   @module rateMe/user
   @description Module for handling authorization (logging in), registration, "state" of our user (is he logged in or not?), getting any sort of individual user information, and any user-related settings/configuration in general.
*/
(function (window) {
    'use strict';
    var angular = window.angular;
    
    
    
    angular.module('rateMe.user', []);
    
    
    
}(window));