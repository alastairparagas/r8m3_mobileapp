/**
    @module rateMe/utility
    @description Handles global utility services (no controllers) for our application that can help in processing certain "global" objects like errors and so forth.
*/
(function (window) {
    'use strict';
    var angular = window.angular;
    
    
    
    angular.module('rateMe.utility', []);
    
    
    
}(window));